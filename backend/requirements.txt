# Full backend runtime + dev/test dependencies.
# For the minimal offline-only set, see requirements-min.txt.
# For ML training (torch etc.), see requirements-train.txt — NOT needed to run
# the API (torch is import-guarded).

# --- Web / API ---
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.5
pydantic-settings>=2.1
python-multipart>=0.0.9
email-validator>=2.0          # EmailStr validation

# --- Persistence ---
SQLAlchemy>=2.0
alembic>=1.13

# --- Security ---
PyJWT>=2.8                    # JWT access/refresh tokens
bcrypt>=4.0                   # password hashing

# --- Reliability / observability ---
tenacity>=8.2                 # retries on LLM calls
structlog>=24.1               # structured JSON logging
prometheus-client>=0.20       # /metrics

# --- Numerics (RAG cosine store fast path) ---
numpy>=1.24

# --- LLM backends ---
anthropic>=0.40               # primary: Claude (streaming)
httpx>=0.27                   # HuggingFace Inference API + TestClient transport

# --- Optional, guarded at runtime ---
# redis>=5.0                  # distributed rate limiting (in-memory otherwise)
# sentence-transformers>=2.2  # real embeddings (heavy; HashingEmbedder default)
# opentelemetry-sdk>=1.24     # tracing (no-op spans without it)

# --- Test / lint / security ---
pytest>=8.0
pytest-cov>=5.0
pytest-asyncio>=0.23
ruff>=0.5
mypy>=1.8
bandit>=1.7
pip-audit>=2.7
