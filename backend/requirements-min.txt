# Minimal dependencies to run the backend fully OFFLINE (MockLLM) and pass tests.
# No vendor SDKs, no heavy ML. numpy is optional (pure-python cosine fallback
# exists) but recommended for speed.
fastapi>=0.110
uvicorn>=0.27
pydantic>=2.5
pydantic-settings>=2.1
httpx>=0.27        # used by Starlette's TestClient
numpy>=1.24
pytest>=8.0
