# Sample Project

A tiny fixture project used by the aiforge backend tests. It contains a
calculator module and an authentication module so the RAG indexer has real,
distinguishable code to chunk, embed, and retrieve.
